export interface RoomItemModel {
    id: number,
    name: string,
    preview: string
} 

export interface RoomModel {
    id: number,
    name: string,
    description: string,
    photo: string,
    audio: string
}